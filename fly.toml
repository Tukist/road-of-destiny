app = "road-of-destiny"
primary_region = "nrt"  # 东京，亚洲访问快

[build]
  dockerfile = "Dockerfile"

[env]
  DATABASE_URL = "sqlite:///./data/game.db"

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1

[mounts]
  source = "game_data"
  destination = "/app/data"
