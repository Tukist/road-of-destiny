app = "road-of-destiny"
primary_region = "sjc"

[build]
  dockerfile = "Dockerfile"

[env]
  DATABASE_URL = "sqlite:///./data/game.db"

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1

[mounts]
  source = "game_data"
  destination = "/app/data"
